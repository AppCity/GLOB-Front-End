const Settings = (props) => {
  return (
    <div className="flex justify-center items-center h-full">
      <span>Settings</span>
    </div>
  );
};

Settings.propTypes = {};

export default Settings;
